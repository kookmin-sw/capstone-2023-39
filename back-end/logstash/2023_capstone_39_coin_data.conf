input {
  file {
    path => "/Users/mj/Desktop/test_coin_data.csv" 
    start_position => "beginning"
    sincedb_path => "/dev/null"
    mode => "read"
  }
}

filter {
  csv {
    separator => ","
    columns => ["inner_ip", "pool_ip", "pool_name", "timestamp", "date"]  
    }

  # mutate {
	# 	convert=>{
	# 		"pool_ip"=>"integer",
  #     "pool_name"=>"string",
	# 		"timestamp"=>"integer",
	# 		# "date"=>"string"
	# 	}
	# }
}

output {
  elasticsearch {
    hosts => ["localhost:9200"]  # Elasticsearch 호스트 및 포트를 적절하게 수정합니다.
    index => "2023_capstone_39_coin_data" # 원하는 인덱스 이름으로 변경합니다.
  }
  stdout {}
}
